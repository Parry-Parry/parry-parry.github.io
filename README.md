# parry-parry.github.io

Andrew Parry's personal site. Jekyll + jekyll-scholar, no other dependencies.

Build locally: `bundle install && bundle exec jekyll serve`. Deploy: pushed to `master`, built and published by `.github/workflows/jekyll.yml`.
